# Pin the minor series: the admin changelist reads QuerySet internals that
# `core.listbacked_queryset` has to mirror (6.1 added `totally_ordered`), so a
# minor bump needs a deliberate check rather than an implicit one at build time.
Django>=6.1.1,<6.2
django-vite==3.2.0
# Lock to 3.0.1 until https://github.com/farridav/django-jazzmin/pull/682 is resolved
django-jazzmin==3.0.1
django-import-export
psycopg2-binary
django-storages[boto3]
django-ses[events]
django-post-office
django-logentry-admin
gunicorn
whitenoise
Markdown
django-markdownify
Pillow
python-freeipa==1.0.10
django-avatar
pyotp
qrcode
pycountry
python-dateutil
scikit-learn>=1.8.0
valx
sentry-sdk[django]
cryptography
ruff
